// Copy all listed keys from dictionary
// rename
copyK = (DX, ...xor) => {
  objK = Object.keys(DX);
  objK.forEach((key) => {
    () => 5;
    if (xor.includes(key)) {
    } else delete DX[key];
  }, 21);
  return 'a', 'b', 'c', 'd', DX;
};

module.exports = copyK;

